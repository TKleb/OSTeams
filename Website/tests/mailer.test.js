import chai, { expect } from "chai";
import mailer from "../services/mailer.js";

describe("Test Mailer", () => {
	// describe("Test sending an email", () => {
	// 	it("It should return the string: Email sent successfully", async () => {
	// 		const response = await mailer.SendMail("gianluca.nenz@ost.ch","testsubject", "<p>In order to use OSTeams</p>");
	// 		console.log(response);
	// 		expect(response).to.include("Email sent successfully");
	// 	});
	// });

	// describe("Test passing invalid options", () => {
	// 	it("It should return the string: There was an error sending your email", async () => {
	// 		const response = await mailer.SendMail("","", "");
	// 		expect(response).to.include("There was an error sending your email");
	// 	});
	// });
});
